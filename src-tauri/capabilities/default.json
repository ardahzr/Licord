{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main Better-VC window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
